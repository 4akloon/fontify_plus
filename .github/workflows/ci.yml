name: CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  analyze-test:
    name: Analyze & test
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        # 3.10 is the package's declared floor; stable catches new lints and
        # deprecations before they reach users.
        sdk: ['3.10', stable]

    steps:
      - uses: actions/checkout@v5

      - uses: dart-lang/setup-dart@v1
        with:
          sdk: ${{ matrix.sdk }}

      - name: Install dependencies
        run: dart pub get --no-example

      # The formatter's output changes between SDKs, so only pin it on stable.
      - name: Verify formatting
        if: matrix.sdk == 'stable'
        run: dart format --output=none --set-exit-if-changed bin lib test

      - name: Analyze
        run: dart analyze --fatal-infos

      - name: Run tests
        run: dart test

      # A second, independent implementation of the OTF/OTF-variable spec.
      # The package's own reader agreeing with its own writer proves nothing
      # about the outside world; this does. Only run once (not once per SDK
      # in the matrix) since it doesn't depend on the Dart SDK version.
      #
      # example/fonts/my_icons.otf is a static font — nothing wires fvar/STAT
      # into the font builder yet. example/fonts/proto/variable_cff2.otf (the
      # format Task 5 chose) is a real variable font, built by fontTools'
      # own varLib rather than this package, but it still exercises the
      # oracle's variable-font branch, which my_icons.otf alone cannot.
      - uses: actions/setup-python@v5
        if: matrix.sdk == 'stable'
        with:
          python-version: '3.12'

      - name: Install fonttools
        if: matrix.sdk == 'stable'
        run: pip install fonttools

      - name: Validate generated fonts against fonttools
        if: matrix.sdk == 'stable'
        run: |
          python3 tool/oracle/validate_font.py \
            example/fonts/my_icons.otf \
            example/fonts/proto/variable_cff2.otf

  publish-dry-run:
    name: Publish dry-run
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: dart-lang/setup-dart@v1
        with:
          sdk: stable

      - name: Install dependencies
        run: dart pub get --no-example

      - name: Dry-run publish
        run: dart pub publish --dry-run
